<div class="container">
  <div class="row">
    <% @cuties.each do |c| %>

          <div class="col-sm-6 col-lg-3 col-xs-12">
            <%= render 'shared/card',cutie: c %>
          </div>

    <% end %>
  </div>
</div>
<!-- link_to, cutie_path(c) -->
