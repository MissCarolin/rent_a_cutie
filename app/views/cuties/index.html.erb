<div class="container">
  <% @cuties.each do |c| %>
    <div class="row">
        <div class="col-sm-6 col-lg-3 col-xs-12">
          <%= link_to cuty_path(c) do %>
            <%= render 'shared/card',cutie: c %>
          </div>
        <% end %>
      </div>
    <% end %>
</div>
