<%= simple_form_for(@cutie) do |f| %>
  <!-- [...] -->
  <%= f.input :name %>
  <%= f.input :species %>
  <%= f.input :description %>
  <%= f.input :booking_rate %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit %>
<% end %>
