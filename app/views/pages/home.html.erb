<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= image_path "lion.jpg" %>')">
  <div class="container-home">
    <h1>Relax you must – end of the world is not.</h1>
    <p>Learn how to unwind with a cutie you want</p>
    <%= simple_form_for :search, url: cuties_path, method: :get do |f| %>
    <div class="search-form-control form-group">
    <input class="form-control string required" input="search" placeholder="find a furry friend" name="search[query]" id="search_query">
    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Find cutie
    </button>
</div>

<% end %>
</div>
</div>

<h1>Meet our favourites <i class="fas fa-paw"></i></h1>
<div class="container">
  <div class="row">
    <% @cuties.each do |c| %>
        <div class="col-sm-6 col-lg-3 col-xs-12">
            <%= render 'shared/card',cutie: c %>
        </div>
    <% end %>
  </div>
</div>
